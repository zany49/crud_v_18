import { createAction } from "@ngrx/store";



export const increment = createAction('Create Increment')
export const decrement = createAction('Create Decrement')
export const reset = createAction('Create Reset')
